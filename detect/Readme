一共包含两个主测试程序：
（1）inference_deepID-v1_time_computing.py 主要测试单幅深度人脸图像的真伪
（2）eval_image_classifier.py 主要测试face_liveness_data数据集中人脸图像的真伪

测试之前，需要对程序的环境变量进行配置：
(1) LD_LIBRARY_PATH   /usr/local/cuda/lib64
(2) 对于单幅图像的测试，需要更改checkpoint以及图像的输入地址， 在inference_deepID-v1_time_computing的第13-15行； 测试时间可直接打印出来
(3) 对数据集图像的地址--dataset_dir、checkpoint_path地址、评估结果保存地址--eval_dir修改，在tf.app.flags.DEFINE_string函数里面更改；测试时间可参看打印的前后时间戳的差值；

更改后尝试运行；
